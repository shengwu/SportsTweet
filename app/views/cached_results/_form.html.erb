<%= form_for(@cached_result) do |f| %>
  <% if @cached_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cached_result.errors.count, "error") %> prohibited this cached_result from being saved:</h2>

      <ul>
      <% @cached_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :result %><br />
    <%= f.text_area :result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
